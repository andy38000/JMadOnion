# GoSkinning ML 重新训练完整流程

## 第一步：下载新版本

### 下载链接
```
https://github.com/andy38000/JMadOnion/archive/refs/heads/cursor/max-c218.zip
```

### 解压后使用
```
GoSkinning_Clean/
├── GoSkinning_Plugin/     ← 3ds Max插件（修复后）
├── ML_Training/           ← 训练代码（修复后）
├── Scripts/               ← 实用脚本
└── 重新训练完整流程.md    ← 本文档
```

---

## 第二步：准备Python环境

### 2.1 确认Python 3.11已安装
```cmd
py -3.11 --version
```
应显示：Python 3.11.x

### 2.2 安装PyTorch（如果没安装过）
```cmd
py -3.11 -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
py -3.11 -m pip install numpy tqdm tensorboard
```

### 2.3 验证GPU可用
```cmd
py -3.11 -c "import torch; print('CUDA:', torch.cuda.is_available())"
```

---

## 第三步：准备训练数据

### 3.1 数据要求

| 要求 | 说明 |
|------|------|
| 格式 | 已蒙皮的 .max 文件 |
| 骨骼 | 统一使用 Biped |
| 网格 | 合并成一个mesh（身体+衣服+头发） |
| 数量 | 建议 500+ 个角色 |

### 3.2 清理数据
每个角色导出前：
- ✅ 保留：身体、衣服、头发
- ❌ 删除：武器、道具、配饰

### 3.3 将 .max 文件放到统一文件夹
```
D:\model_max\
├── character_001.max
├── character_002.max
├── character_003.max
└── ...
```

---

## 第四步：导出训练数据

### 4.1 单个文件导出

1. 在 3ds Max 中打开一个 .max 文件
2. 选中已蒙皮的网格
3. MAXScript → 运行脚本 → 选择：
   `GoSkinning_Clean\ML_Training\data\export_from_max.py`
4. 选择输出文件夹

### 4.2 批量导出（推荐）

在 3ds Max 中运行以下 MaxScript：

```maxscript
(
    -- 设置路径
    inputFolder = @"D:\model_max"           -- .max文件所在文件夹
    outputFolder = @"D:\training_data_v2"   -- 输出文件夹
    
    makeDir outputFolder all:true
    
    maxFiles = getFiles (inputFolder + "\\*.max")
    print ("Found " + (maxFiles.count as string) + " files")
    
    pythonCode = "
import pymxs
import json
import os

rt = pymxs.runtime
OUTPUT_FOLDER = r'" + outputFolder + "'

def export_all():
    for obj in rt.objects:
        skin_mod = None
        try:
            for mod in obj.modifiers:
                if rt.classOf(mod) == rt.Skin:
                    skin_mod = mod
                    break
        except:
            continue
        
        if skin_mod is None:
            continue
        
        try:
            rt.select(obj)
            rt.modPanel.setCurrentObject(skin_mod)
            
            num_verts = rt.skinOps.getNumberVertices(skin_mod)
            num_bones = rt.skinOps.getNumberBones(skin_mod)
            
            if num_verts < 100 or num_bones < 5:
                continue
            
            # 获取骨骼并排序
            bones = []
            bone_names = []
            for i in range(1, num_bones + 1):
                bone_name = rt.skinOps.getBoneName(skin_mod, i, 0)
                bone_node = rt.getNodeByName(bone_name)
                if bone_node:
                    bones.append(bone_node)
                    bone_names.append(bone_name)
            
            if not bones:
                continue
            
            # 按名称排序骨骼
            sorted_indices = sorted(range(len(bone_names)), key=lambda i: bone_names[i])
            old_to_new = {old: new for new, old in enumerate(sorted_indices)}
            sorted_bones = [bones[i] for i in sorted_indices]
            
            # 获取顶点数据
            mesh = rt.snapshotAsMesh(obj)
            vertices = []
            normals = []
            for i in range(1, num_verts + 1):
                pos = rt.getVert(mesh, i)
                try:
                    norm = rt.getNormal(mesh, i)
                    if norm is None:
                        norm = rt.Point3(0, 0, 1)
                except:
                    norm = rt.Point3(0, 0, 1)
                vertices.append([float(pos.x), float(pos.y), float(pos.z)])
                normals.append([float(norm.x), float(norm.y), float(norm.z)])
            
            # 获取排序后的骨骼数据
            bone_data = []
            for bone in sorted_bones:
                head = bone.transform.position
                try:
                    length = bone.length if hasattr(bone, 'length') and bone.length > 0 else 10.0
                    axis = rt.normalize(bone.transform.row3)
                    tail = [float(head.x + axis.x * length),
                            float(head.y + axis.y * length),
                            float(head.z + axis.z * length)]
                except:
                    tail = [float(head.x), float(head.y) + 10, float(head.z)]
                bone_data.append({
                    'name': bone.name,
                    'head': [float(head.x), float(head.y), float(head.z)],
                    'tail': tail
                })
            
            # 获取权重并重映射索引
            weights = []
            for v in range(1, num_verts + 1):
                vert_weights = []
                num_w = rt.skinOps.getVertexWeightCount(skin_mod, v)
                for w in range(1, num_w + 1):
                    bone_id = rt.skinOps.getVertexWeightBoneID(skin_mod, v, w)
                    weight = rt.skinOps.getVertexWeight(skin_mod, v, w)
                    if weight > 0.001:
                        old_idx = bone_id - 1
                        if old_idx in old_to_new:
                            new_idx = old_to_new[old_idx]
                            vert_weights.append([new_idx, float(weight)])
                weights.append(vert_weights)
            
            # 保存
            data = {
                'mesh_name': obj.name,
                'vertices': vertices,
                'normals': normals,
                'bones': bone_data,
                'weights': weights,
                'bone_order': 'sorted_by_name'
            }
            
            output_file = os.path.join(OUTPUT_FOLDER, obj.name + '_skinning.json')
            with open(output_file, 'w', encoding='utf-8') as f:
                json.dump(data, f, ensure_ascii=False)
            
            print('Exported: ' + obj.name)
        except Exception as e:
            print('Skip: ' + obj.name + ' - ' + str(e))
            continue

export_all()
"
    
    for maxFile in maxFiles do (
        print ("Processing: " + maxFile)
        loadMaxFile maxFile useFileUnits:true quiet:true
        python.Execute pythonCode
    )
    
    print "Batch export completed!"
    messageBox "Export completed!" title:"GoSkinning"
)
```

**使用方法：**
1. 复制上面代码到 MAXScript 监听器
2. 修改 `inputFolder` 和 `outputFolder` 路径
3. 执行

---

## 第五步：训练模型

### 5.1 复制训练代码
将 `GoSkinning_Clean\ML_Training` 复制到：
```
C:\Users\Admin\Desktop\GoSkinning_ML_Training_v2
```

### 5.2 开始训练
```cmd
cd C:\Users\Admin\Desktop\GoSkinning_ML_Training_v2 && py -3.11 training/train.py --data_dir D:\training_data_v2 --epochs 200 --batch_size 2 --device cuda --output_dir my_model_v2
```

### 5.3 参数说明
| 参数 | 说明 | 建议值 |
|------|------|--------|
| --data_dir | 训练数据文件夹 | 存放.json的路径 |
| --epochs | 训练轮数 | 200-500 |
| --batch_size | 批次大小 | 1-2（8GB显存）|
| --device | 设备 | cuda |
| --output_dir | 输出目录 | 自定义 |

### 5.4 观察训练
正常训练会显示：
```
Epoch 1/200 | Train Loss: 0.0500 | Val Loss: 0.0000 | LR: 0.001000
Epoch 2/200 | Train Loss: 0.0450 | Val Loss: 0.0000 | LR: 0.001000
...
```

**目标：Loss 降到 0.01 以下**

### 5.5 训练时间估算
| 数据量 | epochs | 预计时间 |
|--------|--------|----------|
| 500 | 200 | 2-4小时 |
| 1000 | 200 | 4-8小时 |
| 2000 | 300 | 8-16小时 |

---

## 第六步：使用训练好的模型

### 6.1 找到模型文件
训练完成后，在输出目录中：
```
my_model_v2/
├── checkpoint_epoch_200.pth   ← 最终模型
├── checkpoint_epoch_100.pth
└── ...
```

### 6.2 复制模型到插件目录
将 `checkpoint_epoch_200.pth` 复制并重命名为 `my_model.pth`，放到：
```
C:\Users\Admin\AppData\Local\Autodesk\3dsMax\2022 - 64bit\ENU\scripts\GoSkinning\models\my_model.pth
```

### 6.3 安装新版插件
用 `GoSkinning_Clean\GoSkinning_Plugin\` 中的文件替换旧插件：
```
复制 GoSkinning_Plugin\GoSkinning\ → scripts\GoSkinning\
复制 GoSkinning_Plugin\GoSkinning_Launcher.ms → scripts\
```

### 6.4 在3ds Max中使用
1. 按 F11 打开监听器
2. 执行：
```maxscript
fileIn @"C:\Users\Admin\AppData\Local\Autodesk\3dsMax\2022 - 64bit\ENU\scripts\GoSkinning_Launcher.ms"
```
3. 在插件界面中选择你的模型 `my_model`
4. 选择网格和骨骼，点击"全局蒙皮"

---

## 常见问题

### Q1: CUDA out of memory
**解决：** 减小 batch_size 到 1

### Q2: Loss 不下降
**解决：** 
- 检查数据质量
- 增加训练轮数
- 降低学习率：添加 `--lr 0.0001`

### Q3: 效果仍然不好
**检查：**
- 训练数据是否用新版脚本导出？
- 插件是否用新版？
- Loss 是否降到 0.01 以下？

---

## 快速命令汇总

```cmd
# 1. 安装依赖（如果需要）
py -3.11 -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# 2. 验证GPU
py -3.11 -c "import torch; print('CUDA:', torch.cuda.is_available())"

# 3. 训练模型
cd C:\Users\Admin\Desktop\GoSkinning_ML_Training_v2 && py -3.11 training/train.py --data_dir D:\training_data_v2 --epochs 200 --batch_size 2 --device cuda --output_dir my_model_v2

# 4. 继续训练（从检查点恢复）
cd C:\Users\Admin\Desktop\GoSkinning_ML_Training_v2 && py -3.11 training/train.py --data_dir D:\training_data_v2 --epochs 300 --batch_size 2 --device cuda --output_dir my_model_v2 --resume my_model_v2/checkpoint_epoch_200.pth

# 5. 查看Loss
py -3.11 -c "import torch; ckpt=torch.load('my_model_v2/checkpoint_epoch_200.pth', map_location='cpu'); print('Loss:', ckpt.get('train_loss'))"
```

---

## 版本信息
- 文档版本：2.0
- 更新日期：2026-02-04
- 主要更新：修复骨骼顺序匹配问题
